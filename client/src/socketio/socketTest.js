/*
import { io } from 'socket.io-client';

const URL = 'http://localhost:5000';
const socket = io(URL, { query: { sessionId: '333' } });

socket.on('connect', console.log('cum'))

socket.onAny((event, ...args) => {
  console.log(event, args);
});
export default socket;
*/
